# StartMath redirect

Public stub that exists only to keep `https://startmath.github.io/startmath/`
working. It redirects every request to https://startmath.gt.tc with the
path preserved.

The actual app lives in the private `startmath/startmath-app` repo.
